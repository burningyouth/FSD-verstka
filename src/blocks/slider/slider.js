import 'bxslider';

$(document).ready(function(){
    $('.room-card__slider').bxSlider({
        wrapperClass: 'slider room-card__slider',
        nextText: 'expand_more',
        prevText: 'expand_more'
    });
});
